# Variáveis de ambiente — nunca commitar
source/app/.env

# Chaves privadas e certificados — nunca commitar (qualquer subpasta)
**/*.key
**/*.pem
certs/
!certs/gerar_certs.sh
!certs/*.cnf

# Python
__pycache__/
*.pyc
venv/
source/venv/

# Logs
*.log
